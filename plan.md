# Sub2API 备份导入计划

## 目标

- [x] 在管理后台上传 `.sql.gz` 备份文件，上传后登记为可恢复记录，并复用现有数据库恢复流程。

## 功能范围

- [x] 新增备份上传接口：`POST /api/v1/admin/backups/upload`。
- [x] 接收 multipart 文件上传，仅允许 `.sql.gz`。
- [x] 将上传文件保存到当前已配置的 S3 兼容对象存储。
- [x] 上传成功后创建 `BackupRecord`，状态为 `completed`。
- [x] 在数据库备份页面增加“导入备份”按钮。
- [x] 上传完成后刷新备份记录列表。
- [x] 恢复时继续使用现有管理员密码确认流程。

## 实现要点

- [x] 复用当前 S3 配置，不新增第二套存储配置。
- [x] 记录文件名使用上传文件名，S3 key 使用现有备份 prefix 并附加记录 ID 避免覆盖同名文件。
- [x] 记录 `size_bytes`、`file_name`、`s3_key`、`started_at`、`finished_at`。
- [x] `triggered_by` 使用 `imported`，用于区分系统自动备份和手动导入备份。
- [x] 上传前校验 S3 配置是否完整。
- [x] 上传后可直接点击现有“恢复”按钮恢复数据库。

## 风险和限制

- 上传会先写入本机临时文件以获得 `Content-Length`，超大文件依赖本机临时目录空间。
- 上传的 `.sql.gz` 必须是兼容当前恢复逻辑的 PostgreSQL SQL dump。
- 暂不处理 Redis 和应用数据目录，只聚焦 PostgreSQL 数据库恢复。

## 测试清单

- [x] 未配置 S3 时上传失败并返回明确错误。
- [x] 非 `.sql.gz` 文件上传失败。
- [x] `.sql.gz` 上传成功后生成可恢复备份记录。
- [x] 上传成功后前端刷新备份列表。
- [x] 导入记录可以复用现有恢复接口。
